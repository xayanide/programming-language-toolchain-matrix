# Programming Languages Tooling Matrix

| Language           | Primary Tools             | Popular Package and Dependency Managers | Popular Build Tools                           | Run Commands                                                            | Runtime Requirement         | Installation Atmosphere       |
| ------------------ | ------------------------- | --------------------------------------- | --------------------------------------------- | ----------------------------------------------------------------------- | --------------------------- | ----------------------------- |
| **ActionScript**   | `mxmlc`, `adl`, `compc`   | `apm (2020)`                            | `ant`, `maven`                                | `mxmlc Main.as`, `adl Main-app.xml`                                     | `Flash Player`, `AIR (SDK)` | Multimedia / Legacy           |
| **Assembly (x64)** | `nasm`, `yasm`, `ld`      | -                                       | `make`, `cmake`                               | `nasm -f elf64 hello.asm && ld hello.o && ./a.out`                      | `OS Kernel`                 | Technical / Manual            |
| **Bash**           | `bash`, `sh`              | `brew`, `apt`, `pacman`                 | `make`                                        | `bash hello.sh`, `./hello.sh`                                           | `Bash Shell`                | Native / Ubiquitous           |
| **BASIC**          | `fbc`                     | -                                       | `make`                                        | `fbc hello.bas && ./hello`, `fbc -run hello.bas`                        | -                           | Minimalist / Retro            |
| **Batch**          | `cmd`                     | -                                       | -                                             | `hello.bat`, `cmd /c hello.bat`                                         | `Windows Command Prompt`    | Native / Legacy               |
| **C**              | `gcc`, `clang`, `make`    | `vcpkg`, `conan`                        | `make`, `cmake`, `ninja`, `meson`, `autoconf` | `gcc hello.c -o hello && ./hello`, `make run`                           | -                           | Industrial / Heavy            |
| **C#**             | `dotnet`, `csc`           | `nuget`                                 | `msbuild`, `dotnet build`                     | `dotnet run`, `csc Hello.cs && ./Hello.exe`                             | `.NET (SDK)`                | Multi-GB (Visual Studio)      |
| **C++**            | `g++`, `clang++`, `cmake` | `conan`, `vcpkg`                        | `cmake`, `ninja`, `make`, `meson`, `bazel`    | `g++ hello.cpp -o hello && ./hello`, `cmake --build . && ./hello`       | -                           | Complex / Fragmented          |
| **C3**             | `c3c`                     | `c3c`                                   | `c3c`                                         | `c3c compile-run main.c3`                                               | -                           | Smooth / Single-Binary        |
| **Carbon**         | `carbon`, `bazel`         | `bazel`                                 | `bazel`                                       | `carbon run hello.carbon`, `bazel run //:hello`                         | -                           | Experimental / Google-centric |
| **Go**             | `go`                      | `go mod`                                | `go build`, `make`, `mage`                    | `go run hello.go`, `go build hello.go && ./hello`                       | -                           | Professional / Polished       |
| **Jai**            | `jai`                     | `jai`                                   | `jai`                                         | `jai main.jai`                                                          | -                           | Exclusive (Closed Beta)       |
| **Java**           | `java`, `javac`, `mvn`    | `maven`, `gradle`                       | `maven`, `gradle`, `ant`, `bazel`             | `java Hello.java`, `mvn exec:java`, `gradle run`                        | `JVM, JRE (JDK)`            | Modular / Enterprise          |
| **JavaScript**     | `node`, `bun`, `deno`     | `npm`, `pnpm`, `bun`, `yarn`            | `vite`, `esbuild`, `webpack`, `rollup`        | `node hello.js`, `bun run hello.js`, `npm start`                        | `Node`, `Bun`, `Deno`       | Instant / High-Speed          |
| **Kotlin**         | `kotlinc`, `gradle`       | `gradle`, `maven`                       | `gradle`, `maven`, `bazel`                    | `kotlinc hello.kt -include-runtime -d hello.jar && java -jar hello.jar` | `JVM, JRE (JDK)`            | Refined / Modern              |
| **Lua**            | `lua`                     | `luarocks`                              | `make`                                        | `lua hello.lua`, `luajit hello.lua`                                     | -                           | Portable / Ultra-light        |
| **Luau**           | `luau`, `rojo`            | `wally`, `pesde`                        | `rojo`, `darklua`                             | `luau hello.lua`, `lune run hello.luau`                                 | -                           | Safety-First / Game-centric   |
| **Odin**           | `odin`                    | `odin`                                  | `odin`, `make`                                | `odin run .`, `odin build . && ./hello`                                 | -                           | Artistic / Game-dev focus     |
| **Pascal**         | `fpc`, `lazarus`          | `opm`                                   | `make`                                        | `fpc hello.pas && ./hello`                                              | -                           | Structured / Academic         |
| **PHP**            | `php`, `composer`         | `composer`                              | `make`, `phing`, `composer`                   | `php hello.php`, `php -S localhost:8000`                                | `PHP Interpreter`           | Web-Standard / Agile          |
| **PowerShell**     | `pwsh`, `powershell`      | `PowerShellGet`, `winget`               | `Invoke-Build`, `psake`                       | `pwsh hello.ps1`, `.\hello.ps1`                                         | `.NET (SDK)`, `PowerShell`  | Native / Admin-Focused        |
| **Python**         | `uv`, `python`, `pip`     | `uv`, `pip`, `conda`, `poetry`          | `setuptools`, `hatch`, `poetry`, `maturin`    | `uv run hello.py`, `python hello.py`                                    | `Python (via uv)`           | Instant (Rust-powered)        |
| **Rust**           | `cargo`, `rustc`          | `cargo`                                 | `cargo`, `maturin`                            | `cargo run`, `rustc main.rs && ./main`                                  | -                           | Robust / Ironclad             |
| **Smali**          | `smali`, `baksmali`       | -                                       | `apktool`, `gradle`                           | `java -jar smali.jar a out/ -o classes.dex`                             | `Android (ART)`             | Hacker / Reverse-Engr         |
| **TypeScript**     | `tsc`, `tsx`, `bun`       | `npm`, `pnpm`, `yarn`, `bun`            | `tsc`, `vite`, `esbuild`, `swc`               | `tsx hello.ts`, `bun run hello.ts`                                      | `Node`, `Bun`, `Deno`       | Strict / Scalable             |
| **WebAssembly**    | `wasm-tools`, `emcc`      | `wapm`, `npm`                           | `emcc`, `wasm-pack`                           | `wasm-tools run hello.wasm`, `node hello.js`                            | `Browser`, `Wasmtime`       | Universal / Sandboxed         |
| **Zig**            | `zig`                     | `zig fetch`                             | `zig build`                                   | `zig run hello.zig`, `zig build run`                                    | -                           | Zero-Dependency / Elite       |
